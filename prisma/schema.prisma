generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model Prompt {
  id        Int      @id @default(autoincrement())
  content   String   @db.VarChar(255)
  resIa     String   @db.VarChar(255)
  createdAt DateTime @default(now())
}
